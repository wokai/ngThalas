import { TimeSegmentsPipe } from './time-segments.pipe';

describe('TimeSegmentsPipe', () => {
  it('create an instance', () => {
    const pipe = new TimeSegmentsPipe();
    expect(pipe).toBeTruthy();
  });
});
